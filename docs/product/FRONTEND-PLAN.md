> ## ⏸ STATUS: DEFERRED
>
> Frontend development is **excluded from the current scope** (backend-first
> strategy — see `docs/product/PRODUCT-ROADMAP.md`). This document remains the
> canonical frontend spec and will be the starting point when UI work begins.

# Ayo-SNBT Frontend — Complete Implementation Plan

> **Purpose:** a self-contained blueprint for building the Ayo-SNBT frontend.
> The backend is already built and running (see `ayo-snbt-backend` repo).
> This plan covers tech stack, project structure, every page with its API
> bindings, state management, auth, WebSocket, payments, and testing.
> A team can implement it in phases without backend design decisions.

---
